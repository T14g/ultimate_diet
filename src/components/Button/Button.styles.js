import styled from "styled-components";

const ButtonStyles = styled.button`
    background: #000;
    color: #fff;
    width: 100%;
    display: block;
    max-width: 300px;
    margin: 10px auto;
    font-weight: 600;
    padding: 10px;

    &:hover {
        cursor: pointer;
    }
`;

export default ButtonStyles;
